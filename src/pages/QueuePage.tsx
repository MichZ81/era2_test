import { GenerationQueue } from "@/widgets/generation-queue";

/** Страница очереди генераций: тонкая композиция без бизнес-логики. */
export default function QueuePage() {
  return <GenerationQueue />;
}
